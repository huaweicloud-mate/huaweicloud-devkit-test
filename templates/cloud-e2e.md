# 真云 E2E 资源台账 — <日期>-<测试主题>

> **红线**：只删**本次创建**的资源；删除/对抗前全量盘点 + 白名单；**禁删既有/他人资源**（2026-09-07 误删教训）。
> **执行时间**：`YYYY-MM-DD HH:mm:ss`（北京时间） ｜ **账号/区域**：`cn-north-4 / hw018619646`

## 一、资源清单（创建记录）

| 资源类型 | 资源ID | 区域 | 用途 | 创建时间 | 最低配置 |
|---|---|---|---|---|---|
| `<ECS/RDS/OBS/FG/...>` | `<id>` | cn-north-4 | `<用途>` | `<时间>` | 是 |

## 二、销毁记录

| 资源 | 销毁命令/动作 | 结果 | 时间 |
|---|---|---|---|
| `<id>` | `<删除命令>` | 成功/失败 | `<时间>` |

## 三、归零验证（复核）

| 资源 | 释放前 | 释放后 | 对比（count/sha256） |
|---|---|---|---|
| `<id>` | `<N 个>` | `0` | 一致 ✅ |

## 四、红线合规声明

- [ ] 只删本次创建资源：`是`
- [ ] 未触碰既有/他人资源：`是`（全量盘点 + 白名单已记录）
- [ ] 删除后归零复核通过：`是`
- [ ] 本台账无明文凭证（AK/SK 已脱敏）：`是`