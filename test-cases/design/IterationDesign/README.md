# IterationDesign 迭代测试设计目录

本目录按**版本**建子目录，存放各版本的**迭代测试设计**。

## 规则

1. 每个被测版本一个子目录（如 `v1.1.3/`）。
2. 版本子目录下放该版本的迭代测试设计文档（测试对象、目标、范围、策略、用例、环境、缺陷管理）。

## 关联

- 设计用例真源：`test-cases/design/`（163 设计级）+ `test-cases/expanded/`（137 展开级）
- 迭代执行结果：`results/Iteration/<版本>/`