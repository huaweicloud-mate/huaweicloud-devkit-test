# #570 码道占位符遮蔽真实凭证 R11 归零（回归用例）

- **关联设计用例**：D2 认证维度（凭证来源/优先级负向）；占位符/脱敏值遮蔽为**新增负向场景**，建议母版新增 D2 负向用例锚定「R11 归零」（待设计阶段与 `design/` 母版对齐具体编号）
- **严重级**：P0（真实凭证被占位符遮蔽 → 云操作全失败 + 诊断误导，凭证正确性 + 功能中断）
- **状态**：**已修复**（1.1.4-next.4，commit `93e5743`；函数级 28/28 + 既有单测 58/58 + 码道真机 testbot2 核心场景 PASS）
- **上游**：https://github.com/huaweicloud/huaweicloud-devkit/issues/570

## 根因

`credentials.mjs` 的 `resolveCredentials()` + `readCodeArtsCredentials()` 只用 **truthy 判断凭证**（`if (ak && sk)`），不识别占位符/脱敏值。码道 IDE 市场安装把 `<HW_ACCESS_KEY>` / `<HW_SECRET_KEY>` 占位符写进 mcp_settings env，被当成有效凭证，遮蔽 S1 中用户真实凭据 → 「连接成功但云操作全失败」，指纹诊断误导。

## 修复实现（commit 93e5743，已含入 1.1.4-next.4）

- 新增 `isPlaceholder()`（`credentials.mjs:66-82`）：尖括号 `<>` / 模板插值 `${}` / 裸关键词 `AK|SK|ACCESS_KEY|SECRET_KEY|TOKEN|REGION`（含 `YOUR_` 前缀）/ `placeholder|replace_me|change.me` / 脱敏值 `前缀3-8位+星号` / 纯星号 → 判定占位。
- 新增 `present()`（`:84-86`）= 非空字符串且 `!isPlaceholder`。
- 四处入口归一：env（`:130-132`）/ S1（`:146-148`、R9 标记门 `:155`）/ CodeArts S4（`:222`、`:245`）/ scanState（`reconcile.mjs:108-114` + `envIsPlaceholder:162`）。
- STS 三件套门用 `present()` 重算（`credentials.mjs:165-166`）→ **占位符三件套永远不算平台注入**。
- 额外容错：S4 server key 宽容匹配 `'HuaweiCloud DevKit'`（`:217`、`:241`）。

## 实测结果（函数级探针，2026-09-14 12:03）

**A 组 `isPlaceholder()` 形态判定 —— 20/20 PASS**

| 形态 | 示例 | 结果 |
|---|---|---|
| 尖括号 / 模板插值 | `<HW_ACCESS_KEY>` / `${HW_ACCESS_KEY}` | ✅ 归零 |
| 关键词 | `YOUR_AK` / `REPLACE_ME` / `change.me` | ✅ 归零 |
| 脱敏值 | `abc****` / `token****` | ✅ 归零 |
| 裸关键词 | `AK`/`SK`/`ACCESS_KEY`/`SECURITY_TOKEN`/`REGION` | ✅ 归零 |
| **真值不误杀** | 20 位字母 / 22 位数字 / 含下划线混合 / 空串 / null | ✅ 不归零 |

**B/C 组应用点 —— 8/8 PASS**

| # | 断言 | 结果 |
|---|---|---|
| B6 | 占位符 env 不遮蔽 S1 真实凭证 | ✅ 返回 S1 真值 |
| B7 | 占位符 S1 置空 → 无有效凭证(null) | ✅ |
| C10 | 占位符三件套 ≠ 平台注入，不遮蔽 S1 | ✅ |
| C13 | 占位符/脱敏 S4 不返回 / 半占位不返回 / server key 宽容匹配 | ✅ |
| — | 真实 S4 正常返回（真值不误杀） | ✅ |

## 边界场景（含码道真机验证，2026-09-14 testbot2）

**已真机验证（码道 testbot2 = 124.70.78.131，aarch64 Ubuntu 24.04，真实 `~/.codeartsdoer` 上下文）**：
- **核心 #570 场景**：码道上下文 + 占位符 `mcp_settings` env + 真实 S1 → `readCodeArtsCredentials` 返回 null（占位符被过滤）、`resolveCredentials` 正确回落 S1（ak=HPU... sk=Exz...）✅
- **占位符 + 无 S1**：`resolveCredentials` 抛 `HDKIT_CRED_MISSING`（占位符不当有效凭证）✅
- **isPlaceholder 真机抽查**：`<HW_ACCESS_KEY>` / `${...}` / `abc****` 归零、20 位真值不误杀 ✅

**已函数级验证（C11/C12，模拟 devspace env 三件套真值，2026-09-14）**：
- **C11（`auth_switch persist` 覆盖 env）** ✅：S1 `configuredBySession=true` + env 三件套真值 → `resolveCredentials` 返回 S1 账号（覆盖 env），符合方案 §12.5。
- **C12（`auth init` 后 env 保持优先）** ⚠️ **SPEC-MISMATCH**：S1 无标记 + env 三件套真值 → `resolveCredentials` 实际返回 S1（账号优先），与方案 §12.1「env 完整真三件套 = 平台注入 → 直接用 env 兜底（`auth init` 不能覆盖 env）」预期相反。根因：`credentials.mjs:165-172` 既有「三件套门」(`envHasFullTriplet && stored → S1 优先`，注释「防 STS 遮蔽」) 语义与方案 v5 的 `env 兜底` 冲突（已上报开发待确认）。

**仍未真机（需真实 devspace 沙盒）**：
- 真实 devspace 沙盒的 `JOB_ENV_HW_*` 平台注入路径未接入真机；C12 SPEC-MISMATCH 已上报开发。
- **C14**（非码道 agent 不误触码道分支）：`isCodeArtsContext()` 查 `.codeartsdoer`/`.codeartswork` 存在性（源码确认非码道无此目录不触发），未单独构造非码道环境实测。

## 回归结论

R11 核心（占位符/脱敏值归零 + 三件套门重算）**已修复且三层验证通过**：函数级 28/28 + 既有单测 58/58 + 码道真机（testbot2 真实 `~/.codeartsdoer` 上下文）核心场景 PASS。#570 根因「占位符遮蔽 S1」在 env/S1/S4 三处入口均已收敛，真值不误杀。修复版本 1.1.4-next.4。**#570 本身可关闭**；另发现 C12 的 SPEC-MISMATCH（三件套门与方案 v5 的 env 兜底语义冲突，`credentials.mjs:165-172`），独立于 #570 核心修复，已上报开发待裁决。