# ITER-002-2026-09-08 未执行用例补测四（D3-B7 审批闭环，基线 1.1.2-next.4）

> 补测时间：2026-09-09 02:35 ｜ 基线：huaweicloud-devkit 1.1.2-next.4（608b120）

## D3-B7 结论：✅ 通过（run_approved_command 审批闭环契约完整）

MCP callTool 等价入口（tools.mjs::callTool → runApprovedCommand）契约验证：

| # | 断言 | 结果 |
|---|---|---|
| 1 | plan 只读 → allow + 签发 token | ✅ |
| 2 | run 只读 + 正确 token + approvedByUser=true → 成功执行 | ✅ |
| 3 | approvedByUser=false → 拒绝（"must be true after explicit user approval"） | ✅ |
| 4 | 伪造 token → 拒绝（"Invalid or expired approval token"） | ✅ |
| 5 | 篡改 args（与 plan 不一致）→ 拒绝（"do not match the approved plan"） | ✅ |
| 6 | **token 一次性**：消费后重放 → 拒绝（"Invalid or expired"） | ✅ |
| 7 | **机制确认**：plan 写命令 deny 时**仍签发 token**（safeToRun=false）→ 供 approve 后 run 执行（plan→approve→run 闭环设计） | ✅ 确认 |

### 关键设计解读
- 写操作链路 = `plan(deny + 签发一次性 token)` → 用户显式 `approvedByUser=true` + 原样 args → `run(allowWrites=true)` 执行
- token 与 args 严格绑定（篡改即拒）+ 一次性（重放即拒）
- **闭环本身安全可靠**（契约层无漏洞）——OBS-9/10/11 缺陷在**客户端/宿主侧**（无人机确认通道/模型可自填 approvedByUser），非本闭环契约问题（与 #557/558/559 triage 结论一致）

### 覆盖更新
- D3-B7 ✅ → 未执行 42 再减 1 = **41**
- 可确证执行：81 + 1 = **82/123（66.7%）**

### 留痕
- 脚本：test-cases/d3-b7-closedloop2.py（callTool 入口，7 断言）；真实写执行闭环已在 OBS-9/10/11 真云实证（单向），未重复建云资源（红线合规）