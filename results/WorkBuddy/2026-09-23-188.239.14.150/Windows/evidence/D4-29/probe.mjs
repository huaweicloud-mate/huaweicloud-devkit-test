// D4-29 - real daily probe (MCP tool call / source import / CLI)
// why: classifyTextCommand(DeleteServers) = {"decision":"deny","risk":"write","reason":"Huawei Cloud write operation blocked until the agent presents a plan and receives explicit user approval."}
