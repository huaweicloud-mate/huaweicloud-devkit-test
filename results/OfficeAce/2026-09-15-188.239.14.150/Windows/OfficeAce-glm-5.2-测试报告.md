# OfficeAce-glm-5.2 每日测试报告

> **报告名**：`OfficeAce-glm-5.2-测试报告.md`
> **生成时间**：`2026-09-15 00:45:48`（北京时间）
> **执行归档**：`results/OfficeAce/2026-09-15-188.239.14.150/Windows/`
> **被测对象**：huaweicloud-devkit（GitHub `huaweicloud/huaweicloud-devkit`）
> **结论**：`PARTIAL`（36 PASS / 45 BLOCKED / 0 FAIL，BLOCKED 为环境限制非产品缺陷）

---

## 一、测试概述

| 项 | 值 |
|---|---|
| 客户端 / Agent | `OfficeAce` + `glm-5.2` |
| OS / 架构 | `Windows AMD64` |
| Node / npm / Python | `Node v24.14.1 / npm 11.11.0 / Python 3.13.4` |
| 被测版本（SUT） | `v1.1.4-next.3`（npm @next） |
| 工具全集 | `40`（MCP tools/list 返回 40 个工具） |
| hcloud / 依赖 | `doctor 11/11 全通过 / status 29 skills 已配置` |
| 真云凭证 | `未使用（本轮无真云 E2E 用例执行）` |
| 测试类型 | 源码级探针 / 真机 CLI（install/doctor/status）/ MCP 协议 |
| 设计真源 | 设计级 179 / 展开级 137 / 追踪表 10 列 |
| daily 基础用例 | 设计级 81 / 展开级 71 |

> **执行方法**：MCP JSON-RPC 2.0 协议探针直调 `huaweicloud-devkit` MCP 服务，CLI 真机执行记录日志，证据统一落 `evidence/<case-id>/`。

---

## 二、执行摘要

| 项 | 值 |
|---|---|
| 计划用例（daily） | `81`（设计级） |
| 已执行 | `81`（全部回填，无空行） |
| PASS / FAIL / BLOCKED / SPEC-MISMATCH / NOT_RUN | `36 / 0 / 45 / 0 / 0` |
| 通过率（分母 = PASS+FAIL+SPEC-MISMATCH） | `100%`（36/36） |
| P0 / P1 / P2 新增缺陷 | `0 / 0 / 0` |
| 红线（I 类）违规 | `0` |
| 资源释放 | `N/A（本轮未创建真云资源）` |

---

## 三、状态汇总

### 3.1 设计级

| 状态 | 数量 | 说明 |
|---|---|---|
| PASS | `36` | 有证据且通过 PASS 门禁 |
| FAIL | `0` | 无 |
| BLOCKED | `45` | 环境阻塞（真云/hook/隔离环境/评测harness），见阻塞项 |
| SPEC-MISMATCH | `0` | 无 |
| NOT_RUN | `0` | 无 |
| **合计** | **`81`** | |

### 3.2 展开级

| 状态 | 数量 | 说明 |
|---|---|---|
| PASS | `4` | 有证据且通过 PASS 门禁 |
| FAIL | `0` | 无 |
| BLOCKED | `67` | 环境阻塞（含非本客户端/OS 用例 23 条） |
| SPEC-MISMATCH | `0` | 无 |
| NOT_RUN | `0` | 无 |
| **合计** | **`71`** | |

---

## 四、缺陷清单

> 本轮测试 **0 FAIL**，无产品缺陷发现。所有 BLOCKED 项均为环境限制（真云凭证/hook 客户端/隔离环境/评测 harness），非产品缺陷。

| # | 级别 | 用例ID | 缺陷描述 | 期望结果 | 实际结果 | 根因 | P/G/I | 状态 |
|---|---|---|---|---|---|---|---|---|
| — | — | — | 无缺陷 | — | — | — | — | — |

---

## 五、阻塞项

| 用例 ID | 阻塞原因 | 环境依赖 | 解除条件 |
|---|---|---|---|
| `D1-5` | uninstall 会破坏当前测试环境 | 隔离环境 | 独立容器中复测 |
| `D1-31` | 需隔离 HOME + 可控版本注入 | 隔离 HOME | 隔离环境到位 |
| `D1-33` | 需可注入路径 + 隔离 HOME | 隔离 HOME | 隔离环境到位 |
| `D1-42` | 需隔离 HOME + 跨进程测试 | 隔离 HOME | 隔离环境到位 |
| `D1-45` | 需双时序注入 + 隔离进程 | 隔离进程 | 隔离环境到位 |
| `D1-58` | 需隔离 HOME + Linux 真机 | Linux 真机 | Linux 环境复测 |
| `D2-10~D2-16` | 需真云多 profile/auth_switch 交互环境 | 真云凭证 + 交互终端 | 真云环境到位 |
| `D3-B1~D3-C5` | 需真云服务操作 / 13 框架样例工程 | 真云凭证 + hcloud CLI | 真云环境到位 |
| `D4-1~D4-24` | OfficeAce 非 hook 客户端，不支持 hook 安全拦截 | hook-capable 客户端 | 切换 hook 客户端或跳过 |
| `D6-1` | 需标准环境 100 次采样 | 标准环境 | 环境到位后复测 |
| `D9-6, D9-9` | 需多客户端互通 / 可注入延迟夹具 | 多客户端 + 夹具 | 环境到位后复测 |
| `D10-1~D10-5` | 需评测 harness + 真实 Agent + 评测预算 | 评测环境 | 评测环境到位 |
| `EXP-*（非 OfficeAce）` | 非本客户端用例 | 对应客户端 agent | 由对应客户端 agent 执行 |
| `EXP-NR3-10/11` | 非本 OS（Linux/macOS）用例 | Linux/macOS 环境 | 对应 OS 环境复测 |

---

## 六、安全与红线合规

- [x] 凭证泄漏事件：`0`
- [x] 写操作误判 read-only：`0`
- [x] 红线（I 类）违规：`无`
- [x] 脱敏复核：证据目录无原始凭证/未脱敏日志

---

## 七、资源释放

| 资源 | 创建 | 销毁 | 归零验证 |
|---|---|---|---|
| 无 | 否 | N/A | N/A |

> 本轮未创建任何真云资源，无需释放。

---

## 八、遗留与建议

- 待裁决 SPEC：无
- 本轮未覆盖（说明范围）：真云 E2E（D2/D3 系列）、hook 安全拦截（D4 系列）、多终端矩阵（Linux/macOS）、评测集（D10）
- 建议：
  1. 真云凭证配置后可解除 D2/D3 系列阻塞
  2. OfficeAce 作为非 hook 客户端，D4 系列建议在测试设计中标注为 N/A 而非 BLOCKED
  3. 评测 harness 就绪后可执行 D10 评测集
