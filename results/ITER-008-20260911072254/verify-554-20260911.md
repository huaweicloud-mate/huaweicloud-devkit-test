# #554 修复状态验证报告（2026-09-11）

> 验证时间：2026-09-11 08:50:00
> 验证对象：huaweicloud/huaweicloud-devkit issue #554（D1-39 Windows P0：spawnSync('npm.cmd') 缺 shell:true → EINVAL 被静默吞掉）
> 验证方法：GitHub API 源码级核查（main/dev 分支 update-check.mjs + setup-cli.mjs）+ issue 状态/评论/PR 关联检索
> 验证账号：@shuangheaven（官方仓 write 权限，GH_TOKEN 隔离在脚本内）

## 一、结论

**❌ #554 未修复。** 三线核查全部为"修复未合入"：

| 维度 | 结果 |
|---|---|
| issue 状态 | **OPEN**（closedAt=null；priority/critical + type/bug；最后更新 2026-09-09）|
| 关联 PR | **无**（gh pr search "554" 空）|
| main 分支源码（commit 2d09d5b）| **未修复**（见下）|
| dev 分支源码 | **未修复**（见下）|
| npm 发布线 | 未发现含修复的新版信号（triage 2026-09-08 转派修复 agent，至今无 PR）|

## 二、源码核查细节

### issue #554 评论时间线
1. **2026-09-08** bot 分类：type/bug + priority/critical + area/ci-cd
2. **2026-09-08 14:51** 开发侧 zhangfajun123：已收到，分析中
3. **2026-09-08 14:56** triage-agent 确认：`setup-cli.mjs` L3057 `spawnSync(npmBin='npm.cmd', ['view', ...], { encoding, timeout: 5000, windowsHide: true })` **缺 shell:true**，L3068 status 判定 + L3069 空 catch 双重静默；install（L3082）/update（L3786）两入口受影响；**处置=转派修复 agent**（建议 undici 直连 或 shell:true，main/dev 双线同步修复）
4. **2026-09-09** @shuangheaven（我方）复验 1.1.2-next.4 (608b120)：**仍复现 EINVAL**

### main 分支（commit 2d09d5b09c39…）：**未修复**
`plugins/huaweicloud-core/src/update-check.mjs` queryDistTagsSync：
```js
export function queryDistTagsSync({ timeoutMs = 5000, cwd } = {}) {
  try {
    const result = spawnSync(NPM_BIN, ['view', 'huaweicloud-devkit', 'dist-tags', '--json'], {
      encoding: 'utf8', timeout: timeoutMs, windowsHide: true, cwd,
    });
    if (result.status !== 0) return null;
    return parseDistTagsOutput(result.stdout);
  } catch { return null; }
}
```
- `NPM_BIN = IS_WINDOWS ? 'npm.cmd' : 'npm'` —— **仍 npm.cmd**
- **shell: 出现 0 次** —— 无 shell:true
- **undici/https/fetch 出现 0 次** —— 未按 triage 建议改直连
- 错误仍 `catch { return null }` 静默
- **结论：main 与 1.1.2-next/608b120 形态完全一致，缺陷原样保留**

### dev 分支：**未修复（部分改善）**
- 同上 spawnSync(NPM_BIN, ...) 无 shell:true、npm.cmd 仍在、无 undici/fetch
- **仅新增 `debugLog()`**（失败时输出 error.message，非完全静默）——改善可诊断性，但 **EINVAL 核心缺陷未解决**（Windows 上 npm.cmd 仍 EINVAL → 更新检测仍失效）

## 三、对测试矩阵的影响（D1-39 / EXP-NR3-09）

- **EXP-NR3-09（D1-39 Windows P0）维持 FAIL**——矩阵状态无需变更，符合 Codex round-14~16 判定
- 放行门槛：**P0 FAIL 未闭合，持续阻断 TEST_DESIGN_READY**（与 Codex 各轮结论一致）
- #554 从 2026-09-08 triage 转派修复 agent 至今（3 天）无 PR 产出

## 四、建议

1. **在 #554 追加复验评论**：main(2d09d5b)/dev 分支 2026-09-11 源码核查仍未获修复，询问修复 agent 进展与 ETA（唯一 P0 阻断项）
2. 保持 EXP-NR3-09 = FAIL 不回退（避免误报已解决）
3. 若修复 agent 无响应，按闭环规范升级（@维护者/重新指派）

## 五、附：核查命令（凭据已隔离）

```
gh issue view 554 -R huaweicloud/huaweicloud-devkit --json state,labels,comments,closedAt
gh pr list -R huaweicloud/huaweicloud-devkit --search "554 in:title,body" --state all
gh api repos/huaweicloud/huaweicloud-devkit/contents/plugins/huaweicloud-core/src/update-check.mjs?ref=main --jq .content
gh api repos/huaweicloud/huaweicloud-devkit/contents/plugins/huaweicloud-core/src/update-check.mjs?ref=dev --jq .content
```