# dev 分支更新与 Issue 解决进度回归核查

- **核查日期**：2026-09-11 14:35（北京时间）
- **被测仓库**：huaweicloud/huaweicloud-devkit（本地工作树 `C:\Users\Administrator\devkit-test\hdk`）
- **核查基线**：dev 分支最新 commit `0316e0076cbd6d1f435432d42fd5aef42d72cd23`（PR #621 合入后），package.json version = **1.1.3-next.4**
- **核查方式**：issue 状态/评论时间线（gh api，GitHub 实时态）+ 关联 PR + **最新 dev 源码逐项核查** + 关键项**本机运行时实测**（#554/#565）

---

## 一、dev 分支更新记录

| 项 | 值 |
|---|---|
| 更新前本地 HEAD | `c6c0965`（chore(release): 1.1.3-next.2 #593） |
| 更新后本地 HEAD | `0316e00`（Merge pull request #621 from guangkunBryant/dev） |
| 版本 | 1.1.2-next.4 → **1.1.3-next.4** |
| ⚠️ fetch 滞后警示 | `git fetch origin dev` 拿到 `306c633`，但 `gh api …/commits/dev` 显示官方真实 HEAD = `0316e00`——**fetch 经镜像/加速代理对象滞后**（与 2026-09-11 早间 #616 验证同款陷阱）。已按处方 `git fetch origin <sha>` 拉取真实对象并核对 `git cat-file` 通过。 |
| 本地 dev 分支状态 | 本地 `dev` 为历史 fork 提交污染分支（混入 lithinkit/coconut1919 等），**未触碰**；主工作树以 detached HEAD 跟官方 dev。 |

近 24h 合并的修复相关 PR：**#600**（sandbox check 行 trim）、**#594**（hook 规则覆盖 Nova*/Reset*）、**#595**（KooCLI config 从 homedir 解析）、**#613**（checkForUpdate 改 fetch registry）、**#621**（codex 安装状态检测改 plugin list JSON）。另有 **#623**（修 #622 代金券凭证错误）**OPEN 未合入**。

---

## 二、Issue 解决进度回归核查表

共核查 open issue 20 个（我方测试提单）。判定口径：源码级核查 + 实测，不凭 issue 状态。

### 已修复 / 主体修复（3 个）

| Issue | 问题 | 修复证据（最新 dev 0316e00） |
|---|---|---|
| **#555** | uninstall --target all 在含 WorkBuddy hook 环境崩溃（TypeError: reading 'PostToolUse'） | `setup-cli.mjs` `uninstallWorkBuddy()` 已加可选链保护：`settings.hooks?.PostToolUse`、`e?.matcher !== '*'`、`settings.hooks.PostToolUse?.length` |
| **#562** | hook_check_deploy_plan 对公网 VPC(0.0.0/0)/公共读写 OBS 零拦截，findings 恒空 | `tools.mjs:1065` 工具实现改为 **真实调用 `evaluateDeployPlan(args.plan)`**；`safety/rules/cloud-risk-rules.json` 新增 deploy_plan stage 规则：`hwc-network-public-admin-port`（0.0.0.0/0 + 管理/DB 端口 → deny）、`hwc-obs-anonymous-write`（匿名写 → deny）|
| **#563** | 全局规则 huawei-agent-rules.mdc 无代码加载/注入（孤儿文件） | 该文件已删除，规则体系整体重构为 `safety/rules/cloud-risk-rules.json`：Node 引擎（`risk-rule-engine.mjs` `loadRiskRules`）与 Python hook（`huaweicloud-safety.py` `first_denied_command_rule`）**双实现加载**，覆盖 command/artifact/deploy_plan 三阶段。⚠️ 遗留：原 MUST 语义"禁直连 CSMS/KMS、密钥任务必加载 huawei-dew"未出现在新 JSON 规则中（仍在 SKILL 提示层） |

### 部分修复（4 个）

| Issue | 问题 | 最新 dev 状态 |
|---|---|---|
| **#554** | Windows 更新检测 EINVAL（spawnSync npm.cmd 无 shell:true） | 主路径 `checkForUpdate()` 改 `queryDistTagsFetch()`（fetch registry + 15s 超时）**绕过 npm.cmd**；但 `queryDistTagsSync`（update-check.mjs:220）、`queryDistTags`（:241）、`upgradePackage`（:394 spawnSync npx.cmd）**仍无 shell:true**。**实测**：三路径 EINVAL 全部复现（详见 §三），加 shell:true 后正常返回 `next: 1.1.3-next.4` → **升级/同步路径仍坏** |
| **#561** | 规则引擎对凭证 env 打印与明文 secret 参数零覆盖 | 已新增 deny 规则：`hwc-command-env-dump`（env/printenv/Get-ChildItem Env: + HUAWEICLOUD→deny）、`hwc-command-credential-file`（cat/type 读 .hcloud/.huaweicloud→deny）、`hwc-command-secret-value-read`（ShowSecretVersion/secret_string→deny）→ **env 打印/凭据文件读取已拦截**；⚠️ 遗留：`--adminPass=` / `--password=` 明文命令行参数仍无规则（原报告样例未全覆盖） |
| **#578** | iac 多缺陷集合（P0 审批 token 跨调用失效 / P0 BSS 缺 RDS/DCS 编码 / P1 0.0.0/0 无 finding） | P1 **已修**（hook deny 规则覆盖 0.0.0/0+管理端口）；BSS **文档缓解**（resource-catalog.md:98 明确 RDS/DCS codes NOT usable+降级指引）；**P0 未修**：`approvalStore` 仍是模块级进程内 Map（hcloud-cli.mjs:17），plan/run 跨进程即失效 |
| **#614** | 升级提醒合并单 5 项 | 项②失败态伪 up_to_date **已修**（`judgeUpdate` 失败返回 `check_failed`，不写冷却）；项③ doQuery 直抛 **已修**（`upgradePackage` 有 try/catch 封装）；项① `hintConsumed` 模块级单例 **未修**（mcp-protocol.mjs:25）；项④ pre 线策略文档**未入库**（docs/superpowers/specs 目录为空）；项⑤ remote 约束未变 |

### 未修复（13 个）

| Issue | 问题 | 最新 dev 关键证据 |
|---|---|---|
| **#502** | auth_switch mode=import 缺省 region + 失败无重放 | `readImportFile()`（tools.mjs:952）**finally 仍无条件 rmSync** 导入文件；mode=import 分支（:1128-1143）仍只校验 ak/sk 不校验 region（persist 返回结构有改进，含 obs.error） |
| **#556** | 卸载回收站删除偶发失败但计数报成功 | 回收站方案（genie-trash）已移除、改直接 rmSync（行为变更）；但 `removed++` 仍不扣失败项（setup-cli.mjs:754 等）——**计数问题依旧** |
| **#557/#558/#559** | 会话级安全族：客户端 MCP 写无可见审批 | 审批门禁架构未变：`approvedByUser` 仍为模型自填参数、`approvalStore` 内存 Map 5min TTL、MCP 子进程不受审批管理；hook 层仅新增命令类 deny 规则，未触及会话审批链 |
| **#564** | evaluateCommandRisk 异常输入 fail-open | `risk-rule-engine.mjs:111` 仍无类型守卫（非字符串经 normalizeText JSON.stringify 后继续评估） |
| **#565** | MCP stdio 畸形 JSON 帧 kill 进程（P1 DoS） | **实测复现**（详见 §三）：mcp-server.mjs:131/:152 `JSON.parse` 无 try-catch，进程 code=1 崩溃 |
| **#570** | env AK/SK 占位符 `<HW_ACCESS_KEY>` 被当有效凭证 | `resolveCredentials()`（credentials.mjs:101-157）对 env 仅 truthy 判断，**无占位符过滤**（R9 configuredBySession 优先仅覆盖 auth_switch persist 场景） |
| **#572** | AK/SK v4 五缺陷（B-03/C-05/F-04/H-05） | B-03 `cmdAuthInit`（setup-cli.mjs:4691）仍直接 writeGlobalCredentials 不备份；C-05 `cmdAuthSync`（:4780）仍无 isTTY 守卫；F-04 `printAuthStatus`（:4628）仍不打印 reconciled；H-05 `syncAuth` 仍单 profile（service.mjs:67 resolveManagedProfile）→ **四项全未修** |
| **#576** | Codex cache 布局 serverInfo.version=0.0.0 | 结构增强：mcp-protocol.mjs:11-22 与 update-check.mjs:134-144 均改 **双级查找**（pluginRoot/packageRoot）→ **疑似修复，待 Codex cache 布局实测确认** |
| **#597** | KooCLI 英文模式 BSS 报 Unsupported service | 服务端/CLI（KooCLI 服务目录）侧问题，客户端无修复痕迹 → 外部依赖 |
| **#615** | 升级测试 5 子问题 | 子问题② `removeOpenCodeConfig`（setup-cli.mjs:584）仍整项删除 `config.mcp['huaweicloud-devkit']`，**丢失自定义 --hdkitservice-endpoint 参数**；①服务端口径、③热重载=架构设计、④老版本 package.json 已不存在于当前版本线 |
| **#622** | 代金券凭证错误被兜底文案吞掉 | `hdkitVoucherStatus/Claim`（hdkitservice-api.mjs:118/:127）catch 仍无条件返回 "Incentive service unavailable"；修复 PR **#623 仍 OPEN 未合入** |

---

## 三、运行时实测证据

### 1. #554 升级/同步路径 EINVAL（Windows 实测，Node v22.23.2）

| 探针 | 结果 |
|---|---|
| `spawnSync('npm.cmd', ['view', …], {windowsHide})`（queryDistTagsSync 形态） | ❌ `error=spawnSync npm.cmd EINVAL` |
| 同命令 + `{shell:true}`（修复形态） | ✅ status=0，`latest: 1.1.3, next: 1.1.3-next.4` |
| `spawnSync('npx.cmd', ['--yes', 'huaweicloud-devkit@latest', 'update', …])`（upgradePackage 形态） | ❌ `error=spawnSync npx.cmd EINVAL` |
| `spawn('npm.cmd', …)`（queryDistTags 形态） | ❌ `error event: spawn EINVAL` |

→ **#554 主检测入口已绕过（fetch），但 MCP 会话内检测（queryDistTags）与升级执行（upgradePackage）在 Windows 上仍完全失效**。

### 2. #565 畸形 JSON 帧 DoS（实测）

- 向 `mcp-server.mjs`（stdio）写入截断的 JSON 帧 `{"jsonrpc":"2.0","id":1,"method":"initialize","params":{`
- 结果：**进程立刻退出 code=1**，stderr 指向 `mcp-server.mjs:113`（JSON.parse 未捕获抛错）
- → P1 DoS **稳定复现**，未修复。

---

## 四、结论与后续建议

1. **dev 分支已更新至 0316e00（1.1.3-next.4）**，近 24h 有 6 个修复 PR 合入，安全规则体系重构（JSON 规则 + Node/Py 双实现）是最大变化。
2. **建议关闭 / 复核关闭**：#555（已修）、#562（已修）、#563（主体已修，建议补充确认 CSMS/KMS 语义）、#576（建议在 Codex cache 布局实测后关闭）。
3. **建议在 #554 上追加复验评论**：主路径已修但升级/同步路径 EINVAL 实测仍复现（附本报告 probe 结果），要求补 `shell:true` 或统一改 fetch。
4. **建议关注 #623**（#622 修复 PR）合入后可复验关闭 #622。
5. 未修复项中 **#565（P1 DoS）、#570（凭证占位符）、#572（auth 4 项）、#578-P0（审批 token）** 均为高优先，建议下一轮迭代集中回归。
6. 本地 `dev` 分支为污染分支（fork 提交混入），建议后续测试基线一律走 detached HEAD + `git fetch origin <gh-api-SHA>`，勿 `git pull` 本地 dev。

## 附：本次核查涉及的源文件

- `src/update-check.mjs`、`src/setup-cli.mjs`、`src/mcp-server.mjs`、`src/mcp-protocol.mjs`
- `src/risk-rule-engine.mjs`、`safety/rules/cloud-risk-rules.json`、`hooks/huaweicloud-safety.py`、`hooks/hooks.json`
- `src/auth/credentials.mjs`、`src/auth/service.mjs`、`src/tools.mjs`、`src/hcloud-cli.mjs`
- `src/sandbox/hdkitservice-api.mjs`、`src/sandbox/uninstall-cleanup.mjs`
- `skills/huawei-iac/references/resource-catalog.md`

探针脚本：`devkit-test\scripts\probe554.mjs` / `probe565.mjs` / `issue-timeline.ps1` / `issue-full.ps1`

---

## 五、Issue 验证评论与关单记录（2026-09-11 14:40 北京时间）

账号：`shuangheaven`（GH_TOKEN 进程内切换，未改 gh 全局 active）。评论正文留档 `results/ITER-009-20260911/comments/`。

| Issue | 动作 | 评论链接 | 关单后状态 |
|---|---|---|---|
| #555 | 验证评论 → 关闭（completed） | https://github.com/huaweicloud/huaweicloud-devkit/issues/555#issuecomment-5630581465 | **closed** |
| #562 | 验证评论 → 关闭（completed） | https://github.com/huaweicloud/huaweicloud-devkit/issues/562#issuecomment-5630582115 | **closed** |
| #563 | 验证评论（披露遗留 CSMS/KMS 语义）→ 关闭 | https://github.com/huaweicloud/huaweicloud-devkit/issues/563#issuecomment-5630582692 | **closed** |
| #554 | 复验评论（部分修复，不关单） | https://github.com/huaweicloud/huaweicloud-devkit/issues/554#issuecomment-5630583214 | open |
| #561 | 复验评论（部分修复，不关单） | https://github.com/huaweicloud/huaweicloud-devkit/issues/561#issuecomment-5630583540 | open |
| #578 | 复验评论（P0 未修，不关单） | https://github.com/huaweicloud/huaweicloud-devkit/issues/578#issuecomment-5630583895 | open |
| #614 | 复验评论（2/5 项已修，不关单） | https://github.com/huaweicloud/huaweicloud-devkit/issues/614#issuecomment-5630584312 | open |

执行脚本：`devkit-test\scripts\issue-close-verify.ps1`。验证口径：评论后 `gh api …/issues/<n> --jq '.state'` + 最后评论者均为 shuangheaven。

### 独立复核（ad-hoc，2026-09-11 14:50）

- 本地断言（`Temp\4\hermes-verify-issueclose.mjs`，node）：changed 脚本存在 ✅、动作矩阵 close=555/562/563+comment-only=554/561/578/614 ✅、7 份评论文件内容标记 ✅、报告记录 7 条评论 ID 一致 ✅ → **4/4 PASS**
- 线上抽查（curl.exe 直连公开 GitHub API，无 token）：评论 5630581465 author=shuangheaven 且正文以「## 测试方复验」开头 ✅；#555/#562/#563 `state=closed`（closedAt=2026-09-11T06:45:31/35/38Z）✅；#554 `state=open` ✅
- 说明：以上为 ad-hoc 验证（非产品测试套件绿）；临时脚本已清理（`Temp\4\hermes-verify-round5.mjs` 非本会话产物，未动）。

## 六、二轮定向实测（dev @0316e00，2026-09-11 15:10 北京时间）

依据一轮核查的判定，对 #554/#570/#576 做了运行时实测（探针 `devkit-test\scripts\probe-iter9.mjs`，Windows 10，Node v22.23.2）。

### 1. #554 —— 缺陷影响面比一轮判定更广（铁证）

| 探针 | 结果 | 判读 |
|---|---|---|
| `queryDistTagsFetch({timeoutMs:15000})` | ✅ `{latest:1.1.3, next:1.1.3-next.4}` | #613 的 fetch 路径在本环境完全可用 |
| `getCachedUpdateInfo('1.1.2')`（默认 doQuery） | ❌ `result=check_failed, target=null` | **MCP 会话内检测主路径仍失效**：`getCachedUpdateInfo` 默认 `doQuery=queryDistTags`（`spawn('npm.cmd')` 无 shell → EINVAL → null → check_failed）。`queryDistTagsFetch` 仅被 CLI 场景 `checkForUpdate()` 使用，未被会话内路径引用 |

结论：`huaweicloud_check_update` 工具与 MCP 预热（mcp-server.mjs `updatePrewarm`）在 Windows 上仍静默失败。#554 **主检测（CLI）绕过、会话内检测与升级路径全失效**，维持 FAIL。

### 2. #576 —— Codex cache 布局实测：缺陷形态仍成立

- 本机存在真实 Codex cache 布局 `~\.codex\plugins\cache\huaweicloud-devkit\{1.1.3-next.2, 1.1.3-next.4}`。
- **两个版本目录均无 package.json**（版本目录与其父目录 `…\huaweicloud-devkit\` 两级都无）→ `readInstalledVersion()`/mcp-protocol 的两级查找必然 miss → `serverInfo.version = 0.0.0`。
- 额外观察：`1.1.3-next.4\src\` 为**空目录**（副本不完整；疑似缓存被清理或安装截断），提示该布局下运行态本身存疑——比 #576 原始报告更进一步，建议在 issue 评论补充（或并入后续观察）。
- 对照组：工作树（npm 标准布局）`readInstalledVersion() = 1.1.3-next.4` ✅。
- 结论：#576 **未修复**（Codex 布局下版本仍 0.0.0）；建议修复方向不变：兜底从插件 manifest（plugin.json）读版本或安装时写入 version marker。

### 3. #570 —— 占位符 env 被接受为有效凭证（缺陷成立）

| 场景 | 结果 | 判读 |
|---|---|---|
| 隔离 HOME（无 S1）+ `HW_ACCESS_KEY='<HW_ACCESS_KEY>'` | ✅ 返回 `ak='<HW_ACCESS_KEY>'`、`sk='<HW_'` | 占位符经 truthy 判断被当作有效凭证——**缺陷形态复现** |
| 真实 S1 + 占位符 env | 返回真实 S1 凭证 | 本机 S1 `configuredBySession=true`（R9）覆盖了 env；但 `auth init` 写入的普通 S1 场景仍会被占位符遮蔽（issue 原始场景） |

结论：#570 **未修复**（`resolveCredentials` 仍无占位符过滤；R9 仅缓解了 persist 场景）。

### 4. 产品自测套件

- ✅ `npm run validate`：29 skills 校验通过，KooCLI 版本配对 7.2.12 通过。
- ✅/⚠️ `npm test`（node --test 全量，Windows 10 实跑）：**410 用例 = 393 pass / 4 fail / 13 skip**（124s）。4 个失败全部位于 `test/agent-install.test.mjs` 代理自动检测/安装段，**均为本机多代理环境与测试 fixture 假设不匹配，非本次 dev 代码回归**：

| 失败用例 | 实测现象 | 归因 |
|---|---|---|
| install auto-detect multiple agents requires explicit target（L573） | 期望命中 `(opencode, workbuddy)`，实际 `(opencode, workbuddy, officeace)` | fixture 仅建 2 个代理目录，本机其他代理检测源泄漏（officeace）→ 检测集合 ≠ 断言 |
| install --target all still installs every supported agent（L684） | exit 1，期望 0 | `--target all` 含 officeace：其安装因 mcp-connectors.sqlite 缺失失败 → 整体 exit 1。**产品注释（L878-881）自证为预期行为**（"OfficeAce cannot fully install in this sandbox…expected"），测试断言 `status===0` 未随该设计对齐 |
| install auto-detect single agent installs only that agent（L713） | 期望单 opencode，实际 `(opencode, officeace)` → exit 1 | 同上：本机代理环境泄漏进仅建 opencode 的 fixture |
| install auto-detect with no agents reports error（L730） | 期望 exit 1，实际 0（检测到代理） | 临时 HOME 隔离未覆盖全部检测源，本机真实代理被扫到 |

- 判定：dev @0316e00 功能测试整体健康（393 pass）；4 个失败归因环境形态（本机 officeace 留存 + `--target all` 含 officeace 的已知部分失败设计），建议上游将 #29 断言改为 `status<=1` 或 fixture 显式排除本机留存代理；不阻塞本轮回归结论。

### 5. 二轮实测结论汇总（对照一轮判定）

| Issue | 一轮判定 | 二轮实测后 |
|---|---|---|
| #554 | 部分修复（主路径绕过） | **影响面升级**：`getCachedUpdateInfo` 默认 doQuery=queryDistTags（spawn npm.cmd）实测 `check_failed`——MCP 会话内检测+qury 预热仍失效；仅 CLI `checkForUpdate` 走 fetch ✅ |
| #576 | 疑似修复（双级查找） | **确认未修复**：真实 Codex cache 布局两级均无 package.json（且 1.1.3-next.4 副本 src/ 空目录），版本读取必回落 0.0.0 |
| #570 | 未修复 | **确认复现**：隔离 HOME 下占位符 env 被接受为有效凭证（R9 仅缓解 persist 场景） |
| 套件 | — | validate ✅；npm test 393/410 ✅（4 fail 归因环境） |