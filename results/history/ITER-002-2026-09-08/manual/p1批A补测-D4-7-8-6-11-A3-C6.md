# ITER-002-2026-09-08 未执行用例补测六（P1 批 A，代码级，基线 1.1.2-next.4）

> 补测时间：2026-09-09 03:05 ｜ 基线：1.1.2-next.4（608b120）

## 结果速览（P1 批 A：18/20 PASS，2 FAIL 为 OBS-12 覆盖缺陷观察）

| 用例 | 结果 | 证据 |
|---|---|---|
| **D4-7 hook 三工具有效性** | ✅ 通过 | check_command/check_artifacts/check_deploy_plan 三工具全部注册 + callTool 可调 |
| **D4-8 Python/Node 策略一致** | ✅ 通过 | 双版本均用 `permissionDecision` + `DENY_PREFIX` + `deny()` 机制；规则规模相近（py≈6 / node≈1 规则簇） |
| **D4-6 adminPass 回显警告** | ⚠️ 缺陷观察（非新） | `--adminPass=` 与 `--new_password=` 明文入参规则引擎 allow——**与 OBS-12 同族（已上报 #561）**，无独立提单价值 |
| **D4-11 提示注入防护** | ✅ 通过 | 拼接命令 `ListServers; rm -rf /` → deny（hwc-sandbox-destructive-command） |
| **D3-A3 沙箱 vs 生产路由** | ✅ 通过 | service_catalog：deploy→huawei-sandbox / create ECS→huawei-ecs 意图路由正确 |
| **D3-C6 沙箱 7 隐式工具具名** | ✅ 通过 | **9/9 具名注册**（connect/upload_file/exec_one_shot/exec_with_session/check_user/credentials/sign_agreement/deploy_nginx/close_session），sandbox_* 族共 **11 个** |

## 断言修正记录（避免误报重复踩坑）

1. D4-8：初始断言 "allow/warn/credential" 字面 FAIL——实际双版本用 `permissionDecision/deny()`，已改为机制级断言 → PASS
2. D3-C6：初始猜测工具名 upload/exec/expose 不存在——实际为 upload_file/exec_one_shot/exec_with_session/deploy_nginx 等，已用真实名 → 9/9 PASS

## 覆盖更新

- P1 批 A 6 用例（D4-7/D4-8/D4-11/D3-A3/D3-C6 通过 + D4-6 观察）→ 未执行 38 − 6 = **32**
- 可确证执行：85 + 6 = **91/123（74.0%）**

## 留痕
- 脚本：test-cases/p1a-probe.py（20 断言含修正）
- D4-6 并入 #561 观察（OBS-12 覆盖），不单独提单