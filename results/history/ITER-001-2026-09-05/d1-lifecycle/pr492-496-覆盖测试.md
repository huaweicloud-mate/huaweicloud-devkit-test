# ITER-001-2026-09-05 PR#492/493/496 开发说明覆盖测试（D1-16~25）

> 输入：开发《卸载/更新/版本查询测试说明》｜ 执行：2026-09-07 ｜ 载体：D1-16~D1-25（新增 10 条用例）｜ 矩阵：221 条（设计 114）

## 覆盖结论：开发说明 3 功能 × 11 测试点 全部覆盖 ✅（2 项部分验证 + 1 新缺口）

## 一、版本查询 `version`（PR#492）

| 开发测试点 | 用例 | 结果 |
|---|---|---|
| 多 agent 只列已装 | D1-16 | ✅ 9 agent 全列出、未装不出现 |
| 全空提示 | D1-17 | ✅ Linux 隔离 HOME 验证（`No Huawei Cloud DevKit plugin installed...`） |
| 版本差异分列 | D1-18 | ✅ 改 Hermes 版本 9.9.9-test → 单独分列；还原正常 |

- ⚠️ **观察项（已按用户指示降级忽略，2026-09-07）**：Codex 插件（CLI 方式）已装但 `version` 不列出——`setup-cli.mjs` 仅处理 codex-desktop 路径。**Codex 桌面版本未安装**，此行为暂定正常（version 面向桌面版目录约定），**不追加上报**；待 Codex Desktop 就绪后复验

## 二、卸载全局清理（PR#493）

| 开发场景 | 用例 | 结果 |
|---|---|---|
| 全局无 flag（非交互）| D1-19 | ✅ 保留提示+flag 指引逐字一致；agent 内容全清；vault 项 N/A（该机无 vault，本机有 vault 未破坏） |
| 全局全量 --clean-global | D1-20 | ✅ vault+obs+hcloud 全删（构造 fake 资产实测） |
| 只删 KooCLI --clean-kocli | D1-21a | ✅ hcloud 删 / obs 留 |
| 只删 OBS --clean-obs | D1-21b | ✅ obs 删 / hcloud 留 |
| 单 agent 卸载 | D1-22 | ✅ 仅 opencode 删 / codearts 留 / 全局保留 + 「如需清理全局配置…uninstall --target all」提示一致 |

- 执行环境：zhangshuang Linux（破坏性 CLI 验证不碰本机 10 客户端资产）；fake 资产构造 → 验证 → 重装复原
- 与早年函数级 D1-10~13 互补：函数层（removeKooCli/removeObsConfig）+ CLI 层（flag/提示/单 agent）双覆盖

## 三、更新检测（PR#496）

| 开发测试点 | 用例 | 结果 |
|---|---|---|
| 当前已是最新 → 无提示 | D1-23 | ✅（next.15 时多次 install 无提示） |
| 旧版 → 黄字提示 | D1-24 | ⚠️ 源码级确认逻辑正确（latest≠current → 黄字）；本机慢镜像下 npm view 5s 超时静默（**恰好实证"非阻塞"设计**）；提示显示待网络良好环境复测 |
| 频道隔离 | D1-25 | ✅ 源码确认（`/-next\.\d+/.test(pkgVersion) ? 'next' : 'latest'`） |

## 四、附带事件

- **基线推进**：npm next 频道已发布 **1.1.1-next.16**（本机 codearts 已随恢复 install 升级；其余 target 仍 next.15）——dev 仓库需拉取比对（待指示）
- 本机 version 现显示混合版本（codearts=next.16 其余=next.15）——真实状态如实呈现（D1-18 分列机制再次佐证）

## 复现脚本

- test-cases/remote-d118.py（D1-17/19）、remote-d120.py（D1-20/21/22）；本机命令见执行记录