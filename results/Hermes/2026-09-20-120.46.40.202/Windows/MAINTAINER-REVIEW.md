# 维护者复核记录（Hermes 2026-09-20 数据质量）

> 本文件由维护者在汇总复核后追加，**非客户端自产**。记录针对 Hermes 今日执行包发现的数据质量问题，不修改客户端标定（CSV「执行状态」列保持客户端原值）。生成时间 2026-09-20（北京时间）。

## 一、git 合并冲突标记污染（已由维护者修复）

- **现象**：`用例矩阵-设计级.csv`、`用例矩阵-展开级.csv` 内残留 `<<<<<<< HEAD` / `=======` / `>>>>>>> f746b91b` 冲突标记（设计级 3 段、展开级 1 段），系客户端在「daily complete」提交时本地 merge 冲突未解决即 `git add -A` 推入。
- **处置**：维护者按「冲突块内保留 incoming 侧（f746b91b = 客户端 P0+P1 complete 回填，含 PASS + 时间戳 + evidencePath）、丢弃 HEAD 侧」消解冲突标记，并对两份 CSV 做了结构完整性校验（设计级 100 行 / 展开级 43 行、无重复 ID、无异常 ID、表头完好）。**未改动任何执行状态标定**。

## 二、执行状态回填缺失（30 条）

- **现象**：设计级 CSV 中 30 条用例「执行状态」列为空，但 `evidence/` 下均有对应证据目录（即实际执行过、证据落盘，仅漏回填状态列）。
- **用例 ID**：`D1-4, D1-30, D1-33, D1-65, D1-66, D1-67, D1-68, D1-69, D2-2, D2-27, D3-B1, D3-B5, D3-C14, D3-S5, D3-S6, D4-10, D4-12, D4-14, D4-25, D4-26, D4-29, D6-1, D6-3, D6-9, D8-1, D8-6, D8-9, D8-10, D9-7, D9-8`
- **影响**：导致报告称 128 PASS，而 CSV 实际仅 98 PASS（设计级 67 + 展开级 31），缺口即这 30 条。**报告自述「已执行 143」与 CSV 回填口径不一致。**

## 三、标 PASS 但 evidencePath 目录缺失（17 条）

- **现象**：设计级 CSV 中 17 条用例「执行状态」= PASS，但 `evidencePath` 列指向的目录在 `evidence/` 下不存在。
- **用例 ID**：`D1-39, D1-40, D2-4, D2-11, D4-1, D4-2, D4-3, D4-5, D4-9, D4-15, D4-18, D4-19, D4-21, D4-22, D4-28, D8-7, D10-4`
- **性质**：疑似证据缺失或证据落点与 `evidencePath` 不符，**待客户端自查**（可能证据以聚合形态存在、未按 `evidence/<case-id>/` 组织）。对照 PASS 门禁（标 PASS 必须①实测②证据落盘③evidencePath 回填），若无法补齐证据则为虚报。

## 四、结论与建议

- 上述三项均为**客户端执行/回填质量缺陷**，不代表被测产品 huaweicloud-devkit 的缺陷。
- 建议由 Hermes 客户端重跑当天流程，补齐 30 条回填 + 复核 17 条 PASS 证据，再交维护者重新汇总。
- 维护者本次仅做「冲突标记消解 + 记录」治理动作，未代执行、未代改标定。