# 问题回归 #615 — DevKit 升级测试：版本校验/配置清理/热重载

| 项目 | 值 |
|------|-----|
| 回归日期 | 2026-09-14 17:10 (北京时间) |
| Issue | https://github.com/huaweicloud/huaweicloud-devkit/issues/615 |
| 回归基线 | 1.1.4-next.6 @69ac727 (npm gitHead) |
| 修复 commit | `d5f4659` — fix(setup): preserve user MCP config fields across install/update/uninstall (#615) (#661) |
| 客户端 | Hermes |
| OS | Windows 10 |
| Node.js | v22.23.1 |
| 测试方法 | 单元测试 + 函数级探针 + 源码审查 |

## 回归结论：部分修复（2/5 已修 + 3/5 非客户端代码缺陷）

| 项 | 问题 | 严重度 | 结论 | 证据 |
|----|------|--------|------|------|
| ① | 测试环境版本校验错误信息不准确 | 高(P1) | **未修（服务端问题）** | `HDKIT_VERSION_TOO_OLD` 不在客户端源码中（grep 零匹配）；错误来自 hdkitservice 服务端，客户端透传 `data.code + data.message`（`hdkitservice-api.mjs:62-68`） |
| ② | 跨大版本降级清理 MCP 配置 | 中(P1) | **已修复** ✅ | `mcp-config-merge.mjs`：install/update 从 overwrite 改为 merge；24 探针断言全 PASS；27 单测全 PASS |
| ③ | MCP 进程无法热重载 | 中(P2) | **未修（架构约束）** | Node.js 进程级约束，文件替换后不自动重载；安装器已提示 "restart session" |
| ④ | 1.0.0 无 package.json | 低(P3) | **未修（历史版本 EOL）** | 1.0.0 已发布无法追溯；1.1.x 全部有 package.json（`version = 1.1.4-next.6`） |
| ⑤ | 跨大版本升级后配置不自动恢复 | 低(P3) | **已修复** ✅ | `mcp-config-backup.mjs`：uninstall 备份 → 重装恢复；per-agent 隔离；`--target all` purge；探针全 PASS |

## 验证详情

### 1. 单元测试（61 用例 = 61 PASS / 0 FAIL）

```
node --test test/mcp-config-merge.test.mjs test/mcp-config-backup.test.mjs test/mcp-config-cli.test.mjs test/hdkitservice-api.test.mjs test/update-check.test.mjs

# tests 61  # pass 61  # fail 0  # duration_ms 3018
```

### 2. 函数级探针（24 断言 = 24 PASS / 0 FAIL）

| 断言 | 结果 |
|------|------|
| Bug2: merge keeps --hdkitservice-endpoint arg | PASS |
| Bug2: merge corrects mcp path | PASS |
| Bug2: merge reports changed (path drifted) | PASS |
| Bug2: identical entry unchanged=false | PASS |
| Bug2: mergeArgsStyle keeps --hdkitservice-endpoint | PASS |
| Bug2: mergeArgsStyle keeps user env FOO | PASS |
| Bug2: mergeArgsStyle adds required env | PASS |
| Bug2: mergeMcpServersFile keeps other-server | PASS |
| Bug2: mergeMcpServersFile keeps --hdkitservice-endpoint | PASS |
| Bug5: extractUserDelta captures extra args | PASS |
| Bug5: extractUserDelta captures custom timeout | PASS |
| Bug5: applyUserDelta restores extra args | PASS |
| Bug5: applyUserDelta restores custom timeout | PASS |
| Bug5: backup file created | PASS |
| Bug5: readAgentDelta returns delta | PASS |
| Bug5: takeAgentDelta returns delta | PASS |
| Bug5: takeAgentDelta consumes (second read null) | PASS |
| Bug5: purgeBackup removes file | PASS |
| Bug5: purgeBackup - file gone | PASS |
| Bug5: opencode delta isolated from codex | PASS |
| Bug5: codex delta isolated from opencode | PASS |
| Bug5: consuming opencode does not affect codex | PASS |
| Bug1: readInstalledVersion returns version | PASS |
| Bug4: current version has package.json | PASS |

### 3. 源码审查

- 修复 commit `d5f4659` 改动 6 个文件（+828/-152 行）：
  - 新增 `mcp-config-merge.mjs`（132 行）— merge 逻辑
  - 新增 `mcp-config-backup.mjs`（70 行）— backup/restore 逻辑
  - 修改 `setup-cli.mjs`（+374/-152 行）— install/update/uninstall 全流程接入 merge + backup
  - 新增 3 个测试文件（404 行）— merge/backup/CLI 集成测试
- 覆盖 agent：opencode, codearts, codearts-work, workbuddy, atomcode, hermes, officeace, openclaw, codex-desktop

## 建议

1. **项②⑤ 可关闭** — 已修复，单测 + 探针全绿，覆盖 9 个 agent。
2. **项① 转服务端 issue** — hdkitservice 服务端修正 `HDKIT_VERSION_TOO_OLD` 错误信息中的最低版本要求（当前提示"≥1.0.0"但实际拒绝 1.1.x）。
3. **项③ 文档化为架构约束** — Node.js 进程级约束，非代码缺陷。
4. **项④ 文档化为历史已知问题** — 1.0.0 EOL，1.1.x 全部有 package.json。
