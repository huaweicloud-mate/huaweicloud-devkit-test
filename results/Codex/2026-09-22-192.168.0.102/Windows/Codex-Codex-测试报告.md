# Codex-Codex 每日测试报告

生成时间：2026-09-22 14:48:00（北京时间）
执行归档：`results/Codex/2026-09-22-192.168.0.102/Windows/`
被测对象：huaweicloud-devkit@1.1.6-next.1，源码 commit `10e5243`
结论：FAIL

## 一、测试概述

| 项 | 值 |
|---|---|
| 客户端 / Agent | Codex |
| OS / 架构 | Windows / x64 |
| Node / npm / Python | Node v22.23.2 / npm 10.9.8 / Python 3.11.15 |
| 工具全集 | 40 |
| 真云凭证 | 已配置；本轮未创建真云资源 |
| 测试类型 | 源码级探针、MCP 工具调用、确定性路由评测 |
| daily 基础用例 | 设计级 100 / 展开级 39 |

执行按 P0→P1→P2 进行。探针输出和逐用例结论落在 `evidence/<case-id>/`。

## 二、执行摘要

| 层级 | PASS | FAIL | BLOCKED | NOT_RUN |
|---|---:|---:|---:|---:|
| 设计级 | 73 | 2 | 25 | 0 |
| 展开级 | 26 | 11 | 2 | 0 |
| 合计 | 99 | 13 | 27 | 0 |

P0 缺陷 1 项，P1 缺陷 2 项。可判定样本通过率为 88.4%（PASS / (PASS+FAIL)）。PASS 门禁与覆盖率门禁均通过。

## 三、状态汇总

设计级共 100 条：PASS 73、FAIL 2、BLOCKED 25。展开级共 39 条：PASS 26、FAIL 11、BLOCKED 2。全部用例已回填执行状态、执行时间；无 NOT_RUN。

## 四、缺陷清单

1. P0 `D4-16`：`sh -c` 包裹的 hcloud 写操作返回 allow，根因 `safety-policy.mjs:428-430`，见 `FINDINGS.md` #1。
2. P1 `D2-4/D4-27`：字符串化 JSON 凭证未脱敏，根因 `safety-policy.mjs:34-46,61-62`，见 `FINDINGS.md` #2。
3. P1 `D10-3`：确定性服务路由评测 11 条 MISS，根因 `tools.mjs:1815-1905`，见 `FINDINGS.md` #3。

## 五、未执行用例与原因

以下均已回填 BLOCKED，分类为【补环境】：

| 用例 | 原因与解除条件 |
|---|---|
| `D1-70` | 缺少独立 proxy/WS 代理验证环境；准备可控代理进程后复测 |
| `D3-C13` | 缺少专用 OBS bucket 写入前置；准备测试 bucket 后复测 |
| `D3-S1/D3-S2/D3-S7/D3-S8` | 缺少对应真云资源、可控失败凭证或隔离资源；准备并归零资源后复测 |
| `D3-S3/D3-C14` | 缺少 Sandbox/DevStation 配额与 hwlink 会话；开通配额后复测 |
| `D3-S4` | 缺少未领取代金券的测试 IAM 账号；准备专用账号后复测 |
| `D9-10/D9-11` | 缺少 remote transport 与 hwlink mux 运行环境；启动隔离服务后复测 |
| `D1-65/D1-66/D1-67/D1-68/D1-69/D2-27` | 本轮未具备对应 CLI/env 专项隔离夹具；准备夹具后复测 |
| `D3-S6` | 缺少 FunctionGraph 配额；准备临时函数与触发器并归零后复测 |
| `D4-25/D4-26` | 缺少客户端 hook runner 的事件遥测运行时；提供 hook runner 后复测 |
| `D4-29` | 缺少独立 hook 命令分类夹具；准备样本夹具后复测 |
| `D6-9` | 缺少已产生缓存的独立进程环境；生成隔离缓存后复测 |
| `D8-10/D8-9` | 缺少带现有 MCP 配置及遥测隔离 HOME；准备隔离 HOME 后复测 |
| `EXP-D5-1-1/EXP-D5-1-3` | 展开级记录依赖真实客户端矩阵，本 Codex 机未提供其他客户端 runner；由对应客户端复测 |

## 六、安全与红线合规

- 凭证泄漏事件：探针使用合成凭证；未向证据写入真实 AK/SK。
- 写操作误判 read-only：0。
- 真云资源：本轮未创建，残留 0。
- PASS 门禁：通过；所有 PASS 有 evidencePath 和本机证据。
- 覆盖率门禁：通过；P0 无空/NOT_RUN，NOT_RUN 占比 0%。

## 七、资源释放

本轮仅执行源码直调、MCP 模拟工具调用和确定性路由评测；未创建 ECS、VPC、OBS、RDS、沙箱或其他真云资源，无需删除，资源残留 0。

## 八、遗留与建议

- 待提单：`FINDINGS.md` 3 项，提单前执行历史 issue 查重。
- 待补测：27 项 BLOCKED，解除条件见第五节。
- 优先修复 P0 shell wrapper 入口和凭证字符串脱敏，再复跑安全探针与路由评测。
