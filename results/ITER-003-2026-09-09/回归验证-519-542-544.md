# ITER-003-2026-09-09 回归验证：#519 / #542 / #544（1.1.2-next.4，608b120）

> 执行日：2026-09-09 ｜ 执行主体：Hermes-Agent（DeepSeek-V4-Flash-0731）
> 被测基线：**huaweicloud-devkit 1.1.2-next.4（608b120）**（与 ITER-003 问题验证同基线，无漂移）
> 修复来源：三 issue 均由 **PR #545**（Fix Codex install, KooCLI auth diagnostics, safety hook）修复，merge 91536d7；`git merge-base --is-ancestor 91536d7 608b120 = True` → **修复全部在发布线内**

---

## 一、回归验证结论总览

| Issue | 修复点 | 验证方式 | 结论 |
|---|---|---|---|
| #519 Codex 插件名错误+误报成功+README 缺指引 | ① 插件名从 marketplace manifest 读取 ② 失败非零退出 ③ uninstall 清理新旧名 ④ README 补 Codex | 源码级 4 点 + runInstallStep 闭环 | ✅ **已修复，无退化** |
| #542 ① codex 写 OpenCode marker ② 沙箱 home 解析 ③ S1/S2 指纹误报 | ① marker codex→null ② hcloud-probe 分类 ③ last_sync 元数据匹配 | 源码 + 功能实测 5/5 + 隔离测试 3/3 | ✅ **已修复，无退化** |
| #544 hook 硬编码 python3 | hooks.json→node safety.mjs + doctor 检查 | 源码 2 点 + #545 相关测试 87/87 | ✅ **已修复，无退化** |

**副作用识别**：#545 改动 24 文件（setup-cli +291、tools -44 重构、新增 hcloud-probe.mjs 等）——未引入新的安全/功能回归（详见 §四）。

---

## 二、#519 Codex 原生插件安装（源码级 4 点验证）

1. **插件名正确**：`getCodexPluginName()`（setup-cli.mjs L620-633）读 `.codex-plugin/plugin.json` 的 `name`（= huaweicloud-devkit），`installCodex` L637 用之 → `codex plugin add huaweicloud-devkit@huaweicloud-devkit`（不再硬编码 huaweicloud-core）。
2. **失败不再误报成功**：r1/r2 非零（非 Access denied）→ `return false`（L653-656/671-674）；`runInstallStep` 收到 false → `installFailures.push` + 不写 marker；`cmdInstall` 末尾 `installFailures.length > 0 → console.error + process.exit(1)`（L3165-3173）→ **安装失败明确报错且非零退出**。
3. **uninstall 清理兼容**：`uninstallCodex` 对 `new Set([pluginName, 'huaweicloud-core'])` 两个名字都 remove（L682）→ 旧版本卸载残留可清。
4. **README 补 Codex/Codex Desktop 指引**：#545 diff 含 README.md/zh-CN.md +22 行（Codex native install/verification guidance）。

> 补充：#545 还修了 `--target all` 部分失败非零退出（#542 评论确认）——本模块验证通过。

## 三、#542 Windows Codex 安装/认证状态（实测）

### 现象 1：install --target codex 不再写 OpenCode marker ✅

`installMarkerDirForTarget`（L3063）：`if (target === 'codex') return null;` → install 成功时 `writeInstallMarker('codex')` = no-op，不再 mkdir `~/.config/opencode/huaweicloud-plugins`。codex-desktop 仍写各自目录（L3073）。

### 现象 2：沙箱 home 解析失败区分 ✅（功能实测 5/5）

新增 `src/hcloud-probe.mjs`（154 行）：
- `findHcloudBin`：HCLOUD_BIN env → `~/hcloud/hcloud.exe` → where.exe / command -v
- `classifyHcloudProbe` 六分类：ok / version_mismatch / sandbox_home_failure（匹配 `获取当前用户家目录失败|home directory|user home|homedir`）/ privacy_pending / not_found（ENOENT）/ unavailable，各带 errorCode + `hcloudProbeNextStep` 行动建议
- 实测（真机 + 构造五场景）：

| 场景 | 预期 | 实测 |
|---|---|---|
| 真机 probeHcloud() | status=ok, v7.2.12 | ✅ ok, installedVersion=7.2.12 |
| 沙箱 home 失败 stderr | sandbox_home_failure | ✅ 3a PASS |
| ENOENT | not_found | ✅ 3b PASS |
| 正常 version 输出 | ok | ✅ 3c PASS |
| 隐私协议待同意 | privacy_pending | ✅ 3d PASS |
| 版本不匹配 | version_mismatch | ✅ 3e PASS |

- `setup-cli` 两处集成（install L3227 / 一次 L3693）：sandbox_home_failure 专门分支提示（"cannot run in this sandbox" + 建议），其余 → "not ready + nextStep"。
- `service.mjs` getAuthStatus/syncAuth 改用 probeHcloud：kooCliInstalled / kooCliStatus / kooCliNextStep 三字段；syncAuth 在 sandbox_home_failure 时给专用错误文案（不再误报 "KooCLI not installed"）。

### 现象 3：auth_status S1/S2-current 指纹误报修复 ✅（隔离测试 3/3）

**修复机制**（reconcile.mjs `s2CurrentMatchesLastDevkitSync` + service.mjs `writeLastSync({kooCliProfile, s1Fingerprint})`）：当 S2-current 的 AK 明文 == S1.ak 且 last_sync 元数据（kooCliProfile 匹配 + s1Fingerprint 匹配 + KooCLI mtime ≤ sync ts+1s）齐备时，判定"此 S2 是 devkit 自己 sync 的"→ effectiveCurrentFp 对齐 S1 → 不再报 S2-current 不一致；无 last_sync 或元数据不匹配 → 行为保持 1.1.1（继续报）。

隔离测试（临时 HOME + HUAWEICLOUD_HOME 注入，KooCLI 模拟 SK 落盘格式转换使指纹 ≠ S1）：

| 场景 | 预期 | 实测 |
|---|---|---|
| A 无 last_sync（1.1.1 基线） | 报 S2-current 不一致 | ✅ s2Count=1, inconsistent=true, currentFp≠s1Fp |
| B last_sync 匹配（devkit 自己 sync） | **不报**（修复目标） | ✅ s2Count=0, inconsistent=false, currentFp==s1Fp |
| C last_sync profile 不匹配 | 仍报（防误吞人工修改） | ✅ s2Count=1, inconsistent=true |

> 注：authEncrypt **整文件加密**（AK 也不可读明文）场景不在本修复范围（那是 #533/AK-FP-1，需解密读取）；本修复覆盖"AK 明文可读但 SK 指纹计算有偏差"即 #542 报告场景。

## 四、#544 Windows Codex safety hook 硬编码 python3（源码级 2 点）

1. **hooks.json 两处 matcher 均改**：`python3 "${CLAUDE_PLUGIN_ROOT}/hooks/huaweicloud-safety.py"` → `node "${CLAUDE_PLUGIN_ROOT}/hooks/huaweicloud-safety.mjs"`（Bash + mcp__huaweicloud 两 matcher）→ 无 Python 环境不再 Hook failed。
2. **Node hook 存在且复用安全策略**：`hooks/huaweicloud-safety.mjs`（1.4KB）`import { classifyTextCommand } from '../src/safety-policy.mjs'`——不再是 python 独有；doctor 检查 Node hook 运行时/配置（#545 评论确认）。
3. **#545 相关测试 87/87 全过**（6 文件：agent-install/hook-node/hcloud-probe/cred-reconcile-e2e/auth-credentials/hook-python）——含新增 hook-node.test.mjs（67 行）、agent-install 146 行增量（覆盖 codex 安装路径假命令回放）。

## 五、副作用识别（是否引入其他问题）

### 5.1 安全回归面（规则引擎/plan 门）——✅ 无变化

- `risk-rule-engine.mjs` 与 `rules/` 在 #545 中**零改动**（git diff 空）→ 规则语义无漂移。
- 本机规则引擎冒烟（5 命令）：destructive `rm -rf /` → deny ✓；`DeleteServer` → warn（**既有行为**，非 #545 引入；plan 门仍 deny 兜底）✓；只读 allow ✓；`echo $HW_ACCESS_KEY` 仍 NO-RULE（OBS-12 已知盲区不受影响）✓；`OBS mb` 规则层 allow + plan 门 deny（既有 wrapper 兜底）✓。
- plan 门冒烟：ECS 创建 → deny/risk=write/safeToRun=false ✓；只读 → allow ✓。
- `classifyTextCommand` 新增 `applyRawCommandRiskRules`：非华为云命令也过 evaluateCommandRisk（#544 Node hook 需要）——**加固**，不缩小既有放行面。

### 5.2 T1 全量单测——✅ 无 #545 引入失败

- 全量 npm test：337 用例多次运行 331~336 pass / 6~1 fail，**失败均为 `mcp-server.test.mjs` 的 "Timed out waiting for initialize" 偶发超时**（单文件跑 0 失败；该文件不在 #545 改动 24 文件内）→ **Windows 全量 runner 时序串扰，环境项非回归**。
- #545 改动的 6 个测试文件独立跑 **87/87 全过**。

### 5.3 工具面——✅ 无删减

- tools.mjs -44 行 = check_cli 的 hcloud 探测逻辑**抽出到 hcloud-probe.mjs**（重构复用，非删工具）；D5-3 枚举 39 工具 = 37（既有）+ check_update/upgrade（#525 会话级更新检测新增，非 #545）。
- install 流程新增探测输出（kooCliStatus/kooCliNextStep）为加字段，不破坏既有调用。

### 5.4 观察项（非缺陷，记录）

- authEncrypt 整文件加密场景的 S2 误报（#533 族）不在 #545 修复范围，与 #542 现象 3 是不同根因（前者密文读法、后者 SK 格式转换）——保持 #533 跟踪。
- codex-desktop 与 codex 双 target 的 marker 语义不同（前者写目录、后者 null 用 Codex 自身状态）——符合 #542 期望（"Codex 原生插件安装成功后应使用 Codex 自身安装状态作为 marker"）。

## 六、结论

**#519/#542/#544 三个已关闭 issue 在 1.1.2-next.4（608b120）全部修复到位，未引入其他问题（安全规则零改动、工具零删减、T1 相关测试全过、全量失败为环境偶发）**。修复质量良好：既有缺陷（OBS-12 等）不受影响，新增分类（sandbox_home/privacy/version_mismatch）为纯增强。

证据脚本：`evidence/verify-519-542-544.mjs`（probe 5 场景）、`evidence/verify-542-isolated.mjs`（S1/S2 3 场景）、`evidence/verify-519-sideeffects.mjs`（规则/plan 冒烟）。